/**
 * Configuración básica de Amplify para conexión manual
 * Este archivo se puede usar como referencia para configurar Amplify manualmente
 */

// Configuración básica de ejemplo (no se usa automáticamente)
export const amplifyConfig = {
  // Configuración de autenticación (si es necesaria)
  Auth: {
    // Configuración de Cognito si se usa
  },

  // Configuración de datos
  Data: {
    // Configuración para el cliente de datos
  },
};

export default amplifyConfig;
